---
- hosts: localhost
  remote_user: root
  roles:
    - asterisk
    - role: Yannik.php
      php_install_versions: ['5.6']
    - geerlingguy.composer
    - ansible-role-freepbx
  vars:
    asterisk_db_password: passw0rd
